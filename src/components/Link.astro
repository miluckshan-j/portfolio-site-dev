---
interface Props {
  link?: string;
  content?: string;
  disableIcon?: boolean;
  boldText?: boolean;
}

const { link, content, disableIcon } = Astro.props;
---

<a
  href={link}
  class=`font-medium ${disableIcon ? "px-3.5 py-2.5 hover:bg-slate-100 rounded-md" : "decoration-2 hover:underline hover:decoration-cyan-900"} no-underline hover:text-cyan-900`
>
  {content}
  {disableIcon ? null : " ↗"}
</a>
